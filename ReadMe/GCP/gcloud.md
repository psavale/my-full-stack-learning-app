# Gcloud commands

```markdown
1. Configure Google Cloud
    - brew cask install google-cloud-sdk
    - Add entries to the shell profile when using Bash
2. Install Pub/Sub emulator
    - gcloud components install pubsub-emulator
2. Install Cloud Datastore emulator
    - gcloud components install cloud-datastore-emulato
3. 
```
